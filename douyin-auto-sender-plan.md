# 抖音自动续火项目规划书

## 一、项目目标

自己重新实现一个轻量、可控的抖音聊天自动发送工具，核心功能保持一致：

1. 使用已有 Cookie 登录抖音。
2. 根据配置搜索目标用户。
3. 自动打开聊天窗口。
4. 发送指定消息或随机消息。
5. 记录执行结果。
6. 失败时保存截图和错误日志。

第一版只关注本地运行，不加入过多通知、定时和复杂配置。

## 二、建议移除的功能

根据需求，可以暂时去掉：

- GitHub Actions 定时运行
- 钉钉通知
- 邮件通知
- 自动 Fork、Star 等项目引导
- 复杂的一言数据结构
- 过多浏览器启动参数
- 不必要的兼容逻辑
- 发送完成后强制等待和交互式关闭
- 与上游项目相关的内容

第一版只保留：

```text
读取配置
启动浏览器
注入 Cookie
搜索用户
发送消息
输出结果
保存失败截图
```

## 三、技术方案

建议使用：

```text
Python 3.11+
Playwright
python-dotenv
```

项目类型：

```text
Python 命令行工具
```

推荐使用 Playwright 的异步 API `async_playwright`。浏览器操作包含大量等待、跳转和异步行为，异步写法更适合后续扩展。

## 四、项目结构

建议结构：

```text
douyin-auto-sender/
├── app/
│   ├── __init__.py
│   ├── main.py              # 程序入口
│   ├── config.py            # 配置读取和校验
│   ├── browser.py           # 浏览器启动和 Cookie 注入
│   ├── douyin.py            # 抖音页面操作
│   ├── message.py           # 消息生成
│   └── logger.py            # 日志处理
├── data/
│   └── messages.json        # 自定义消息列表
├── artifacts/
│   └── .gitkeep             # 失败截图和运行产物
├── .env.example
├── .gitignore
├── requirements.txt
├── README.md
└── run.py
```

如果想保持简单，第一版也可以先只使用 `run.py`、`config.py` 和 `douyin.py`，不要一开始拆分过多模块。

## 五、配置设计

建议使用 `.env` 配置基础参数：

```dotenv
DOUYIN_COOKIE='[...]'
DOUYIN_TARGET_NAMES='["用户A", "用户B"]'
MESSAGE_TEXT='你好'
MESSAGE_FILE=data/messages.json
HEADLESS=true
BROWSER_PATH=
```

配置含义：

| 配置 | 作用 |
|---|---|
| `DOUYIN_COOKIE` | 抖音登录 Cookie |
| `DOUYIN_TARGET_NAMES` | 目标用户列表 |
| `MESSAGE_TEXT` | 固定发送内容 |
| `MESSAGE_FILE` | 随机消息文件 |
| `HEADLESS` | 是否显示浏览器 |
| `BROWSER_PATH` | 本机浏览器路径 |

消息建议支持两种模式。

### 固定消息

```dotenv
MESSAGE_TEXT='今天也要开心呀'
```

### 随机消息

```json
[
  "今天也要开心呀",
  "记得早点休息",
  "晚上好"
]
```

如果同时配置固定消息和消息文件，建议固定消息优先。

## 六、核心执行流程

### 1. 启动程序

程序启动后：

1. 加载 `.env`。
2. 校验 Cookie。
3. 校验目标用户列表。
4. 加载消息内容。
5. 启动 Playwright。

配置错误时直接退出，并给出明确提示。

### 2. 注入 Cookie

将 Cookie JSON 转换成 Playwright 格式，然后执行：

```text
启动浏览器
创建 BrowserContext
添加 Cookie
打开抖音聊天页面
```

不建议在代码中保存真实 Cookie，也不要把 `.env` 提交到 Git。

### 3. 检查登录状态

打开聊天页后，应先判断是否成功登录：

```text
已出现聊天搜索框 -> 登录成功
跳转登录页或没有搜索框 -> 登录失败
```

登录失败时保存截图，输出“Cookie 失效或登录失败”，然后停止执行。

### 4. 搜索目标用户

对每个目标用户执行：

```text
清空搜索框
输入用户名称
等待搜索结果
匹配完整名称
点击发消息
```

搜索不到用户时，记录失败对象并继续处理下一个用户，最后统一输出结果。

### 5. 发送消息

进入私信页面后：

```text
定位编辑框
点击编辑框
输入消息
按 Enter 发送
等待发送完成
```

第一版只支持纯文本，不处理图片、视频、表情和文件。

### 6. 输出结果

最终输出：

```text
成功：用户A、用户B
失败：用户C
跳过：用户D
```

同时保存运行日志，例如：

```text
artifacts/run.log
artifacts/failure-screenshot.png
```

## 七、页面定位策略

抖音页面 DOM 结构可能变化，因此不要只依赖非常具体的 CSS 类名。

建议定位优先级：

1. `placeholder`
2. `aria-label`
3. 可见文本
4. 相对稳定的 `data-*` 属性
5. CSS 类名作为最后手段

建议将页面选择器集中放在一个地方。后续抖音页面变化时，只修改选择器，不修改业务流程。

## 八、错误处理

需要处理以下情况：

| 场景 | 处理方式 |
|---|---|
| Cookie 未配置 | 启动前报错 |
| Cookie JSON 格式错误 | 启动前报错 |
| 目标用户为空 | 启动前报错 |
| Cookie 失效 | 保存截图并退出 |
| 搜索不到用户 | 记录失败并继续 |
| 找不到发消息按钮 | 记录失败并继续 |
| 找不到编辑框 | 保存截图并继续或终止 |
| 发送超时 | 记录失败 |
| 浏览器启动失败 | 输出启动错误 |

推荐定义结果状态：

```text
success
failed
skipped
```

不要只使用 `True` 或 `False`，后续排查会比较困难。

## 九、第一阶段开发计划

### 阶段一：基础运行

目标：

- 能读取 `.env`
- 能启动 Playwright
- 能注入 Cookie
- 能打开抖音聊天页

验证标准：浏览器可以正常打开并进入聊天页面。

### 阶段二：单用户发送

目标：

- 搜索一个用户
- 打开私信
- 发送一条固定消息

验证标准：可以成功向指定用户发送文本。

### 阶段三：多用户发送

目标：

- 支持用户数组
- 单个用户失败不影响后续用户
- 输出成功和失败列表

验证标准：用户 A 失败时，用户 B 仍然可以继续执行。

### 阶段四：随机消息

目标：

- 支持 `messages.json`
- 每个用户随机选择一条消息
- 支持固定消息模式

### 阶段五：日志和截图

目标：

- 保存运行日志
- 失败时截图
- 输出清晰的错误原因

### 阶段六：配置完善

最后再考虑：

- 无头模式
- 自定义浏览器路径
- 自动关闭浏览器
- 定时任务
- 钉钉通知
- Docker 或 GitHub Actions

不要在第一版同时实现这些功能。

## 十、测试计划

### 配置测试

测试以下情况：

- 缺少 Cookie
- Cookie JSON 错误
- 目标用户为空
- 目标用户格式错误
- 消息文件不存在
- 消息文件为空

### 浏览器测试

测试以下情况：

- Cookie 能否成功注入
- 登录状态是否有效
- 搜索框是否能定位
- 编辑框是否能定位
- 消息是否发送

### 失败测试

测试以下情况：

- 不存在的用户
- 失效 Cookie
- 页面加载超时
- 浏览器异常关闭
- 网络异常

## 十一、后续扩展顺序

推荐按这个顺序扩展：

1. 本地固定消息发送
2. 本地随机消息发送
3. 多目标用户
4. 日志和截图
5. 浏览器配置
6. 定时任务
7. 通知功能
8. Web 管理界面

不要一开始就开发 Web 面板。这个项目的核心难点是抖音页面自动化，应先确保命令行版本稳定。

## 十二、最终定位

项目可以定位成：

> 一个轻量的、个人使用的抖音聊天自动发送工具。

设计原则：

- 配置少
- 依赖少
- 默认行为明确
- 出错信息清晰
- 不依赖上游仓库
- 不强制第三方通知
- 本地可以独立运行
- 后续需要时再接入定时任务

另外，当前参考项目使用 GPL-3.0 许可证。如果只是参考功能并重新独立实现，尽量不要直接复制原项目代码、注释和文档结构；如果复制或修改了原项目代码，则需要遵守 GPL-3.0 的开源和版权要求。
